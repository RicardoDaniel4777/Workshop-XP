version: '3.8'

services:
  xp-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xp-dashboard
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
